# text decoration set
Aviutl2のテキスト装飾セット

テキストの簡単な設定をリストから選択できます。
数値や色は変更を行えません。

構造プリセットを選択した場合、数値や色を変更できます。

.gitattributes
README.md
の2つ以外は、すべて動作に必要なので、すべて同じフォルダに入れてください。

既知の不具合:
文字を個別オブジェクトにして、グラデーションや縁取りの指定文字実行を使用すると非常に重くなる。私の環境では落ちた。
コードを書き換えて新たなプリセット項目を新しく登録すると、プロジェクトファイルの文字設定がリセットされる。
